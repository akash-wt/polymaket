[package]
name = "db"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = {version= "0.9.0",features = [ "postgres", "runtime-tokio-native-tls", "macro","uuid"]}
uuid = { version = "1", features = ["v4", "serde"] }
dotenvy = {workspace=true}
anyhow = {workspace = true}